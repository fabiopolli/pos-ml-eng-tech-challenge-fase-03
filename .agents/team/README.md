# Equipe

| Identidade | Branch | Liderança | Revisão esperada |
|---|---|---|---|
| Fábio Polli | `fabio` | arquitetura inicial, CI/CD, Docker, testes e documentação | revisa arquitetura, contratos, Compose e CI |
| Denis Melo | `denis` | EDA, dataset e Airflow | revisa contrato e pipeline de dados |
| Will (Bill) | `bill` | modelo, otimização e observabilidade | revisa contrato/artefato do modelo e métricas |
| Romário | `romario` | API, arquitetura em nuvem e vídeo | revisa contrato HTTP e proposta de deploy |

O agente cruza a identificação informada no pedido com `git config user.name` e `user.email`. Em caso de ambiguidade, pergunta antes de criar a branch. Não existe arquivo de “usuário atual”.

